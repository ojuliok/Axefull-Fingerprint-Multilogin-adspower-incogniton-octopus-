{
  "buildCommand": "npm run build:renderer && mv dist/renderer renderer",
  "outputDirectory": "renderer",
  "rewrites": [
    {
      "source": "/(.*)",
      "destination": "/index.html"
    }
  ]
}
